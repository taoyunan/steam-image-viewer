# Steam 游戏素材快速处理工具 (Steam Game Asset Fast-Processor)

专为游戏编辑、社区运营人员和内容创作者设计的在线工具，旨在通过自动化流程，一站式快速获取和处理 Steam 游戏的各类官方图片素材与关键信息。

**[➡️ 访问在线工具](https://taoyunan.github.io/steam-image-viewer/123.html)**  


---

## 🆕 最新更新

-   **初始页精简**：页面首次打开时只显示 Steam App ID / 商店链接输入框，成功读取数据后再展开横幅编辑、API 数据、宣传影像和图片探查器。
-   **左侧快速导航与状态栏**：固定导航菜单可一键跳转到横幅、基础数据、配置、宣传影像、图片探查器和链接导出区域，并在左侧显示获取状态、缺失项和搜索历史。
-   **顶部商店预览卡片**：成功读取后展示商店头图、开发商、发行商、价格、平台、类型、年龄分级、Metacritic、地区价格摘要等重点信息。
-   **基础数据增强**：补充开发商、发行商、官网、价格、地区价格、平台、分类、发布日期、PC 配置等常用信息。
-   **地区价格与好评度**：新增多地区本地币种价格表，并按语言/地区展示 Steam 好评率、好评/差评数量和总评测数。
-   **宣传影像独立展示**：视频区域从图片探查器中拆出，作为独立模块展示视频封面和播放/复制/下载入口。
-   **高清视频封面**：自动将 Steam 返回的 `movie_600x337.jpg` 封面替换为 `movie_full.jpg`，优先展示 1920x1080 高清封面图。
-   **新版视频格式支持**：兼容 Steam 新版 `HLS 自适应流`、`MPEG-DASH H.264`、`MPEG-DASH AV1` 字段，并自动解析最高分辨率，如 `1920x1080`、`2560x1440 2K`、`3840x2160 4K`。
-   **接口容错优化**：SteamCMD、商店详情、好评度、地区价格、截图、图片、视频等接口或素材部分失败时，页面会尽量继续展示已成功获取的数据，并在左侧说明未获取到的内容。

接口、字段和页面位置词条的维护说明见：[功能与 API 对照表](./API_MAPPING.md)。

---

## ✨ 主要功能

本工具深度整合了 Steam 的两大核心 API，覆盖了从信息获取、素材下载到最终出图的全过程。

### 1. 自定义横幅生成器 (Banner Creator)
-   **智能加载**: 自动加载游戏的高清背景图，并**优先加载简体中文版 (`schinese`) 的 Logo**。
-   **默认居中**: Logo 加载后默认放置在画布正中央，为您提供一个干净的构图起点。
-   **文字定制**: 自动填充游戏名称，并支持多行自定义文字、字体、颜色、大小和行距调整。
-   **自由布局与快捷键**: 
    -   直接用鼠标拖动 Logo 或文字进行精细微调。
    -   按住 <kbd>Shift</kbd> 键可**同步拖动** Logo 和文字。
    -   当元素重叠时，按住 <kbd>Ctrl</kbd> 键可**强制只拖动文字层**，解决选择困难。
-   **一键保存**: 完成设计后可直接将画布导出为高质量的 PNG 图片。

### 2. 图片探查器 (Image Inspector)
-   **全面搜刮**: 自动深度解析 API 数据，找出所有与该游戏相关的图片资源链接（**包含官方商店截图**、库图、社区图等）。
-   **智能分组**: 所有图片按“分辨率”自动归类，让你能快速找到特定尺寸的素材。
-   **用途分组切换**: 需要按用途查看时，可切换为截图、Logo、Library Hero、封面、背景图等用途分类。
-   **一键下载所有截图**: 新增专用按钮，一键批量下载所有从商店获取的高清截图，并自动命名。
-   **批量导出**: 支持一键复制所有图片链接或将其导出为 `.txt` 文件，方便存档或用于其他工具。

### 3. 宣传影像查看器 (Video Inspector)
-   **独立视频区域**: 与图片探查器同级展示，不再混在图片分类列表中。
-   **高清封面**: 自动使用 `movie_full.jpg` 展示视频封面，支持打开、复制和下载封面图。
-   **多格式视频流**: 支持 MP4、WebM、HLS、MPEG-DASH H.264、MPEG-DASH AV1 等字段。
-   **分辨率识别**: 自动读取 HLS/DASH 清单并显示最高分辨率，便于快速判断视频素材质量。

### 4. 高级图片裁剪 (Advanced Cropping)
-   **手动精裁**: 内置强大的在线图片裁剪工具，支持自由选择、固定长宽比 (16:9, 4:3 等) 裁剪，并可指定输出分辨率。
-   **一键自动化 (核心功能)**: 提供“**自动裁剪封面图**”功能，只需一次点击，即可自动完成以下所有任务：
    1.  智能寻找 `920x430` 分辨率的横向主图（优先使用中文版）。
    2.  将其**自动居中裁剪**并下载为 `474x242`, `497x278`, `300x180` 三种常用新闻或专区封面尺寸。
    3.  智能寻找 `300x450` 分辨率的纵向封面图（同样优先中文版）。
    4.  直接下载这张 `300x450` 的**原始图片**。

### 5. API 数据浏览器 (API Data Explorer)
-   **中文优先**: **优先从 Steam 中文商店获取所有文本信息**，包括“关于游戏”、“简介”等，内容所见即所得。
-   **丰富信息**: 全面展示来自两大 API 的关键数据，现在包含：
    -   关于游戏 (`about_the_game`)
    -   游戏简介 (`short_description`)
    -   PC 配置要求 (最低与推荐)
    -   游戏官网、标签、分类
    -   开发商、发行商、官网、价格、地区价格、平台、分类、发布日期
    -   各语言/地区好评度
-   **结构化展示**: 将复杂的 JSON 数据整理成清晰、易读的表格，并对关键字段进行汉化。
    -   宣传影像不再重复塞进 API 数据表，而是在独立“宣传影像”区域展示。

## 🚀 如何使用
1.  **获取 App ID**: 前往任意游戏的 Steam 商店页面，其网址末尾的数字就是 App ID (例如 `https://store.steampowered.com/app/570/Dota_2/` 中的 `570`)。
2.  **输入并获取**: 将 App ID 或完整商店链接粘贴到首页输入框中，点击“获取”按钮。
3.  **等待数据展开**: 数据读取成功后，页面会自动展开横幅编辑、基础数据、宣传影像和图片探查器等完整功能区。
4.  **快速跳转与状态查看**: 使用左侧导航菜单，一键跳转到“基础”“配置”“视频”“图片”“链接”等区域；左侧会显示获取状态、缺失内容和历史记录。
5.  **生成横幅**: 在页面上方的画布区域调整你想要的横幅效果，完成后点击“保存图片”。
6.  **查看宣传影像**: 在“宣传影像”区域查看高清视频封面、复制封面链接，或打开 HLS / MPEG-DASH 高清视频流。
7.  **处理图片**: 向下滚动到“图片探查器”区域：
    -   点击“**自动裁剪封面图**”按钮，一键下载4张处理好的关键图片。
    -   点击“**一键下载所有截图**”按钮，批量保存所有官方截图。
    -   在下方列表中找到你需要的单张图片，进行下载或裁剪。
8.  **查看数据**: 如果需要，可以点击“查看 API 数据”按钮来深入了解游戏的基础数据、地区价格、好评度、配置和语言支持。

## 部署和使用
这是一个纯前端的单文件应用，无需任何后端服务。
-   **本地使用**: 直接下载本仓库中的 `123.html`，用浏览器打开即可。
-   **在线部署**: 将 `123.html` 文件上传到任何静态网站托管服务，如 GitHub Pages, Vercel, Netlify 等，即可拥有一个公开的在线版本。

## 致谢
-   **数据来源**: [steamcmd.net API](https://api.steamcmd.net/) & [Official Steam Store API](https://store.steampowered.com/)
-   **裁剪功能库**: [Cropper.js](https://github.com/fengyuanchen/cropperjs)
-   **代理服务**: [CORS Proxy](https://corsproxy.io/)
